<?php
$pageTitle = 'Рекомендації';
require __DIR__ . '/includes/header.php';
?>
<section class="card shadow-sm">
    <div class="card-body p-4">
        <h1 class="h3 brand">Рекомендації розробникам</h1>
        <p>Нижче — вимоги до web-розробки, захищеної від SQL-ін’єкцій. Це відповідь на питання 4 і пункт 8 лабораторної роботи.</p>
        <ol class="lh-lg">
            <li><strong>Ніколи не склеюйте SQL з введенням користувача.</strong> Текст запиту має бути константою, а дані — окремими параметрами.</li>
            <li><strong>Використовуйте prepared statements</strong> (PDO, mysqli, SQLAlchemy, тощо) або ORM, який параметризує запити за замовчуванням.</li>
            <li><strong>Не показуйте сирі помилки СУБД</strong> у відповіді сервера. Користувач бачить загальне повідомлення, деталі пишуться лише в журнал.</li>
            <li><strong>Валідуйте типи.</strong> Для <code>id</code> — лише цифри; для логіна — обмежена довжина й алфавіт. Це додатковий шар, не заміна параметрів.</li>
            <li><strong>Хешуйте паролі</strong> (password_hash / bcrypt / argon2). У цій навчальній роботі паролі навмисно відкриті, у реальному проєкті так робити не можна.</li>
            <li><strong>Мінімальні права облікового запису БД:</strong> web-додаток не повинен бути власником усієї бази й не повинен виконувати довільний DDL, якщо це не потрібно.</li>
            <li><strong>Екрануйте вивід у HTML</strong> (<code>htmlspecialchars</code>), щоб дані з БД не стали XSS. Це інший клас атак, але його часто ловлять разом із SQLi.</li>
            <li><strong>WAF і фільтри символів — не захист.</strong> Їх можна ставити як додатковий контроль, але коректний запит до БД все одно має бути параметризованим.</li>
        </ol>
        <p class="mb-0">У цьому стенді виправлення зроблені в <code>login.php</code>, <code>register.php</code> і <code>news.php</code>: <code>prepare()</code> + <code>execute()</code>, перевірка <code>ctype_digit</code> для ідентифікатора новини, без виводу тексту SQL-помилки.</p>
    </div>
</section>
<?php require __DIR__ . '/includes/footer.php'; ?>
